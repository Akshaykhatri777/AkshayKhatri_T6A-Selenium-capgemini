# find_element(locator_name,locator_value)  -- single element, returns single element, If no such element Exception
# find_elements(locator_name,locator_value) -- multiple, return list of web element, If not present return empty list

Locators - address of an element
1. ID
2. NAME
3. CLASS NAME
4.TAG NAME
5. LINK TEXT
6. PARTIAL LINK TEXT
7. CSS SELECTORS
8. XPATH

After finding element we can perform 2 Actions --  click() & send_keys('text')

CSS Selectors - syntax:
  'tag-name[attribute = 'value']'
Ctrl + f ---  verify- 1 of 1, code-highlighted, element-highlighted

